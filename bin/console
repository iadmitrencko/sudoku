#!/usr/bin/env php
<?php

require __DIR__ . '/../vendor/autoload.php';

use Sudoku\Command\HelloCommand;
use Symfony\Component\Console\Application;

$app = new Application('Sudoku', '1.0.0');
$app->addCommand(new HelloCommand());
$app->run();
